<%= form_for(@bloco) do |f| %>
  <% if @bloco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bloco.errors.count, "error") %> prohibited this bloco from being saved:</h2>

      <ul>
      <% @bloco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :sigla %><br>
    <%= f.text_field :sigla %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
